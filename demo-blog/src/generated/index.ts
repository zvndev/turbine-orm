/**
 * Auto-generated by turbine-orm — DO NOT EDIT
 *
 * Generated at: 2026-03-26T05:03:13.507Z
 * @see https://github.com/zvndev/turbine-orm
 */

import { TurbineClient as BaseTurbineClient, QueryInterface } from 'turbine-orm';
import type { TurbineConfig } from 'turbine-orm';
import { SCHEMA } from './metadata';
import type { ArticleRecommendation, ArticleTag, Article, Author, Category, Comment, Site } from './types';

/**
 * Generated Turbine client with typed table accessors.
 *
 * Tables:
 *   - `articleRecommendations` (article_recommendations)
 *   - `articleTags` (article_tags)
 *   - `articles` (articles)
 *   - `authors` (authors)
 *   - `categories` (categories)
 *   - `comments` (comments)
 *   - `sites` (sites)
 *
 * @example
 * ```ts
 * const db = turbine({ connectionString: process.env.DATABASE_URL });
 * const rows = await db.articleRecommendations.findMany();
 * ```
 */
export class TurbineClient extends BaseTurbineClient {
  /** Query interface for the `article_recommendations` table */
  declare readonly articleRecommendations: QueryInterface<ArticleRecommendation>;
  /** Query interface for the `article_tags` table */
  declare readonly articleTags: QueryInterface<ArticleTag>;
  /** Query interface for the `articles` table */
  declare readonly articles: QueryInterface<Article>;
  /** Query interface for the `authors` table */
  declare readonly authors: QueryInterface<Author>;
  /** Query interface for the `categories` table */
  declare readonly categories: QueryInterface<Category>;
  /** Query interface for the `comments` table */
  declare readonly comments: QueryInterface<Comment>;
  /** Query interface for the `sites` table */
  declare readonly sites: QueryInterface<Site>;

  constructor(config?: TurbineConfig) {
    super(config, SCHEMA);
  }
}

/**
 * Create a new Turbine client instance.
 *
 * @param config - Connection configuration. Falls back to DATABASE_URL env var.
 * @returns A fully-typed TurbineClient with table accessors.
 */
export function turbine(config?: TurbineConfig): TurbineClient {
  return new TurbineClient(config);
}

export * from './types';
export { SCHEMA } from './metadata';
